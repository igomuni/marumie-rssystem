# rs-vis `76c6f09` が触ったファイルに、marumie 側で修正が入ったか

比較: marumie `3c0224f`（#294 マージ後） ↔ rs-vis `1f69d0a`
対象: `76c6f09`（事業バブルチャート＋共通ナビ＋政策評価の統合強化）が変更した **64ファイル**

## 結論

| 状態 | 件数 |
|---|---|
| **完全一致**（as-is のまま） | **53** |
| 差分あり | 10 |
| 両方に存在しない | 1（`app/model-lab/page.tsx`） |

差分ありの10件は性質が2つに分かれる。

- **A: 取り込み時に marumie 側で手を入れた — 4件**
- **B: 76c6f09 とは無関係に marumie が元々進んでいた — 6件**

`app/model-lab/page.tsx` は `76c6f09` で追加され `1f69d0a` で削除されたため、marumie では最初から取り込んでいない。

## A. 取り込み時に手を入れた4件

### `app/sankey-svg/page.tsx`（差分 1,779行）

`76c6f09` の変更は `+410 -223`。このうち **PR-3 で3ハンク（`+106 -216`）を見送った**。取り込んだのは `+304 -7`。

見送ったのは右上コントロール群の再構成:

```text
@@ -3991,24 +4254,6   中央クラスタ（年度セレクタ＋探索履歴）の削除
@@ -4326,6 +4605,91   表示設定(⋮) を検索ボックス隣へ移動
@@ -4381,207 +4745,30  右上クラスタの全面置換（ツール - 年度 - メニュー）
```

marumie の右上は AIチャットパネル展開時に `rightControlsOffset` ぶん左へ退避する制御・`isCompactWidth` 分岐・探索履歴の入口を持ち、rs-vis にはこれらが無い。そのまま持ち込むと退避ロジックと衝突し年度セレクタが二重になる。`PageNavMenu` は表示設定ボタンの左隣に置いて機能だけ取り込んだ。

また、スコア足きりは rs-vis のインライン実装をそのまま採らず、marumie が抽出済みの `buildFilterExcludedIds` に事業単位フック `excludeProject?: (projectId: number) => boolean` を足す形にした（インライン版を採ると抽出が巻き戻り、再委託フィルタが失われるため）。

差分1,779行の大半は marumie 独自機能（AIチャット・探索履歴・再委託フィルタ・共有パネル部品）で、`76c6f09` 由来ではない。

### `app/api/quality-scores/route.ts`（差分 210行 / +43 -167）

`76c6f09` は型定義もローダも同居した旧形の route に `startYear`・`endYear`・`noEndDate`・`yearsRunning` と `attachDuration()` を足している。marumie は #242 以降 `app/lib/api/quality-scores-loader.ts` へ分離済みで `/api/quality-scores/[pid]`・`/api/search/projects` も共用しているため、rs-vis 版をそのまま採ると分離が巻き戻り他2ルートが壊れる。

**`76c6f09` の実際の変更だけをローダ側へ移植した**（読み込みは `tryReadDataJson` に置き換え）。差分167行の削除分は、rs-vis 側にだけ残る旧形の重複定義。

### `app/quality/page.tsx`（差分 26行 / +17 -9）

PR-2 で rs-vis 版（1,925行）を as-is 採用したため、残差は marumie の型・資産に合わせた3点のみ。

| 箇所 | 内容 |
|---|---|
| `RecipientRow` の import 元 | `@/app/api/quality-scores/recipients/route` → `@/app/lib/api/quality-recipients-loader`（marumie では型の正典がローダ側） |
| `execAmount` の null | marumie の型が `number \| null`。`formatAmount()` 5箇所と `buildPolicyEvaluations()` 入力で `?? 0`。実データは2年度とも null が0件で挙動は変わらない |
| `STATUS_META` の `cn` | marumie が #243 で追加した法人番号一致のステータス。rs-vis 版に無く型エラーになるため追加 |

いずれも marumie 側のローダ・型が rs-vis に無いことに起因する。反映時に一緒に送れば解消する。

### `app/subcontracts/page.tsx`（差分 137行 / +113 -24）

`76c6f09` は年度変更時に `router.replace` を呼ぶが、marumie は #263 で `history.replaceState` により年度を含む全状態をURL同期済み。重複するため **`router.replace` は入れていない**。

差分の大半（+113）は marumie の URL=状態化そのもので、`76c6f09` 由来ではない。

## B. 76c6f09 とは無関係な既存差分 6件

`76c6f09` が同ファイルを触ってはいるが、その内容は取り込み済みで、差分は marumie が前から持っていたもの。

| ファイル | 差分 | marumie 側にあるもの |
|---|---|---|
| `scripts/decompress-data.sh` | 32行 | 展開対象の限定・`data/server` 同期（#231/#242）。＋ PR-1 で足した `rs*-project-outcomes` のバンドル除外 |
| `docs/quality-scoring-guide.md` | 25行 | Google検証版スクリプトの節（#235）、旧5軸からの移行理由 |
| `CLAUDE.md` | 11行 | 応答スタイル・Agent行動ルール（#285 の Opus 5 プロンプト統制）、`components/` のレイヤ記述 |
| `package.json` | 8行 | `vitest`・`test`・`check-traces`・`react-markdown`・`remark-gfm` |
| `scripts/score-project-quality-ai.py` | 2行 | docstring の参照先を task doc → `docs/quality-scoring-guide.md` へ（#285） |
| `.gitignore` | 1行 | `docs/image*.png` |

## 一致している53件（as-is のまま）

```text
新規ページ・API   app/project-bubble/page.tsx · client/components/ProjectMap/BubbleCanvas.tsx
                  app/api/{project-map,policy-summary,execution-history}/route.ts
                  app/lib/{policy-evaluation,project-map,project-map-view}.ts
                  types/{policy-decision,project-map}.ts
共通ナビ          components/navigation/PageNavMenu.tsx
                  app/mof-budget-overview/page.tsx · app/sitemap.ts
フィルタ部品      components/filters/{MinMaxInput,MultiSelectDropdown}.tsx
パイプライン      scripts/{generate-project-map.py,generate-project-outcomes.py,
                  benchmark-embedding-models.py,analyze-*.ts,export-*.ts,
                  summarize-quality-benchmark-run.ts,validate-quality-benchmark.ts}
データ            public/data/{project-map-2025,project-quality-scores-2025,
                  rs2025-project-outcomes}.json.gz
ドキュメント      docs/{policy-decision-scoring,quality-evaluation-benchmark}.md
                  docs/exports/*.csv · docs/tasks/*（5本）
テストデータ      tests/benchmark-results/*(10) · tests/fixtures/*(3)
コマンド          .claude/commands/data-update.md
```

`components/filters/MinMaxInput.tsx` は当初「`〜`→`~` は表記の後退」として取り込まなかったが、as-is 維持の方針決定後に取り込んで一致させた。

## 補足

`76c6f09` 由来のコードには既知の不具合が18件残っている（修正は `fix/policy-eval-corrections` に退避、一覧は `20260728_0741_rs-vis由来の既知不具合18件の棚卸し.md`）。**上記の「一致53件」はそれらの不具合を含んだまま一致している**という意味である。

## 方針決定（2026-07-30）

- **完全一致53件**: 修正しない。既知不具合18件を含んだまま維持する
- **差分あり10件**: 個別に rs-vis へ寄せられるなら寄せる
- **両方に存在しない1件**（`app/model-lab/page.tsx`）: `1f69d0a` で rs-vis から削除済み。**対応不要**

## 差分10件の「寄せられるか」判定

| ファイル | rs-vis のみ | 判定 |
|---|---|---|
| `CLAUDE.md` | 1行 | **寄せる** |
| `app/sankey-svg/page.tsx` | 706行 | **設計判断が要る**（右上の再構成を採るか） |
| `package.json` | 1行 | 寄せる意味なし（末尾カンマのみ） |
| `.gitignore` | 0行 | 寄せる対象なし |
| `docs/quality-scoring-guide.md` | 1行 | 寄せない |
| `scripts/score-project-quality-ai.py` | 1行 | 寄せない |
| `scripts/decompress-data.sh` | 7行 | 寄せない |
| `app/quality/page.tsx` | 9行 | 寄せない（反映で自然解消） |
| `app/subcontracts/page.tsx` | 24行 | 寄せない |
| `app/api/quality-scores/route.ts` | 167行 | 寄せない |

### 寄せる — `CLAUDE.md`（1行）

Layer Design Rules の UI Components 行が rs-vis 側のほうが正確。

```text
rs-vis  | UI Components | `client/components/` / `components/` | 再利用可能UI。直接APIコール禁止（`components/` はフィルタUI等の共通部品） |
marumie | UI Components | `client/components/`                | 再利用可能UI。直接APIコール禁止 |
```

marumie にも `components/filters`・`components/navigation`・`components/subcontracts` が実在するので、**marumie 側の記述が古い**。rs-vis に合わせる。

### 設計判断 — `app/sankey-svg/page.tsx`

rs-vis のみ706行のうち、**意図的に見送ったのは右上コントロール群の再構成3ハンク**（`+106 -216`）。残りは marumie が置き換えた旧実装で、寄せる対象ではない。

寄せる＝［ツール - 年度 - メニュー］配置と表示設定(⋮)の検索ボックス隣移動を採用すること。ただし marumie の `rightControlsOffset`（AIチャット退避）・`isCompactWidth`・探索履歴の入口を、その構成の中へ作り直す必要がある。**UI をどちらの形にしたいかの判断が先**。

なお PR-3 の後始末（`offsetControlsBlock` 117行の未使用・lint 新規警告2件）も同じ領域なので、着手するならまとめて片付く。

### 寄せない7件の理由

| ファイル | 理由 |
|---|---|
| `app/api/quality-scores/route.ts` | 寄せる＝ローダ分離(#242)の巻き戻し。`/api/quality-scores/[pid]`・`/api/search/projects` が壊れる |
| `app/subcontracts/page.tsx` | rs-vis のみ24行は marumie が URL=状態化(#263)で置き換えた旧 useState 群と `router.replace`。寄せると二重同期になる |
| `app/quality/page.tsx` | 差分3件は marumie のローダ・型（`quality-recipients-loader`・`execAmount: number \| null`・`cn`）が rs-vis に無いことが原因。**marumie を rs-vis へ反映すれば自然に消える**ので、marumie 側で直す必要はない |
| `scripts/decompress-data.sh` | 寄せる＝サーバAPIしか読まないデータの展開を復活させること。関数バンドル制御(#231/#242)の前提が崩れる |
| `docs/quality-scoring-guide.md` / `scripts/score-project-quality-ai.py` | 寄せる＝docstring の参照先を task doc へ戻すこと。「コードから `docs/tasks/` を参照しない」ルール(#285)に反する |
| `package.json` | 末尾カンマの有無のみ。marumie 側の追加依存（vitest 等）は反映時に送る側 |
| `.gitignore` | rs-vis のみの行は0。marumie の `docs/image*.png` を送るだけ |

## 実施順の提案

1. `CLAUDE.md` の1行を rs-vis に合わせる（単独で完結・低リスク）
2. `app/sankey-svg/page.tsx` の右上をどうするか決める。採るなら PR-3 の後始末と同時に実施
3. 残る8件は marumie が正であり、`.agents/skills/sync-rs-vis` で rs-vis へ反映すれば解消する側
