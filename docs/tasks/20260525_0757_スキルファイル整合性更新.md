# スキルファイル整合性更新 — リリース整理連動

作成日: 2026-05-25 07:57 JST
対象ブランチ: `chore/release-cleanup`
親計画: [20260525_0540_リリース向けプロジェクト整理実施計画.md](20260525_0540_リリース向けプロジェクト整理実施計画.md)

## 目的

`chore/release-cleanup` で削除した不要ページ・スクリプトに、`.claude/commands/` 配下のスキルファイル4件が古い参照を残しており、次回 agent が誤って削除済みコマンドを呼び出す事故を防ぐため整合性を取る。

## 対象ファイルと検出された不整合

### `.claude/commands/pipeline.md`

| 古い参照 | 状態 |
|---|---|
| `normalize_csv.py`, `npm run normalize` | スクリプト削除済み |
| `generate-structured-json.ts`, `npm run generate-structured` | 削除済み |
| `generate-sankey2-data.ts`, `npm run generate-sankey2` | 削除済み |
| `compute-sankey2-layout.ts`, `npm run compute-sankey2-layout` | 削除済み |
| `build-houjin-db`, `build-houjin-lookup` | 削除済み |
| `data-pipeline-guide.md` | 残存だが旧情報を含む可能性 |
| `check-project-state.py` | 削除済み |

→ 残存5生成スクリプト（generate-sankey-svg / generate-subcontracts / generate-mof-data / generate-project-details / score-project-quality）に絞った内容に書き換え。

### `.claude/commands/data-update.md`

| 古い参照 | 状態 |
|---|---|
| `npm run normalize`, `npm run generate-structured` | 削除済み |
| `rs2024-structured.json{,.gz}` 中心の手順 | データそのものが削除済み |
| `allowed-tools` の対応 `npm run` も削除対象 | 削除済み |

→ 公開4ページのデータ更新フロー（generate-sankey-svg / generate-subcontracts / generate-mof-data / generate-project-details / score-quality）に書き換え。

### `.claude/commands/sankey.md`

| 古い参照 | 状態 |
|---|---|
| `/sankey`セクション、`/sankey2`セクション | ページ削除済み |
| `app/sankey/page.tsx`, `app/sankey2/page.tsx` | 削除済み |
| `app/lib/sankey-generator.ts` | 削除済み |
| `app/api/sankey/route.ts` | 削除済み |
| `client/components/Sankey2/*` | 削除済み |
| `types/preset.ts` への link | 削除済み |
| `docs/sankey-architecture-guide.md` | 残存（/sankey 中心の記述だが今回は更新スコープ外） |

→ `/sankey-svg` のみを対象とする内容に絞り込み。

### `.claude/commands/investigate.md`

| 古い参照 | 状態 |
|---|---|
| `analyze-*.{py,ts}` 全件 | 削除済み |
| `investigate-*.py` 全件 | 削除済み |
| `assess-spending-validity.py` | 削除済み |

→ 紹介できる調査スクリプトがほぼ存在しないため、スキル自体を **DELETE**（CLAUDE.md Skills 表からは既に除外済み）。

## 実施内容

1. `pipeline.md` 書き換え
2. `data-update.md` 書き換え
3. `sankey.md` から `/sankey`・`/sankey2` セクション削除、レイヤーテーブル簡略化
4. `investigate.md` 削除

## 検証方法

- `grep -rn "sankey2\|generate-structured\|normalize_csv\|build-houjin\|generate-entity\|investigate-houjin\|investigate-unlabeled\|analyze-" .claude/commands/` で残存参照ゼロを確認
