# rs-vis への反映対応案（2026-04-29）

## 現状

- **前回反映**: `565f2c5`（2026-04-22）
- **rs-vis 最終コミット**: `dac7076`（2026-04-22）
- **今回反映対象**: `565f2c5`〜`2b8cf6a`（2026-04-22〜04-28）

---

## 変更ファイル一覧（docs/ 除外済）

| ファイル | 内容 |
|---|---|
| `app/sankey-svg/page.tsx` | 府省庁フィルタ・支出先なしノード・検索改善・ミニマップ・Pin機能・フィルタUI 等 |
| `app/lib/sankey-svg-filter.ts` | フィルタロジック更新 |
| `client/components/SankeySvg/MinimapOverlay.tsx` | ミニマップUX改善 |
| `types/sankey-svg.ts` | 型定義追加 |
| `scripts/generate-sankey-svg-data.ts` | データ生成スクリプト更新 |
| `public/data/sankey-svg-2024-graph.json.gz` | データ更新 |
| `public/data/sankey-svg-2025-graph.json.gz` | データ更新 |
| `README.md` | 更新あり（要確認） |

変更は `/sankey-svg` に集中。`app/sankey/page.tsx`・`app/lib/sankey-generator.ts` 等は変更なし。

---

## 実施手順

### Step 0: rs-vis にブランチを作成

```bash
cd /Users/igomuni/MyGitHub/rs-vis
git checkout -b sync/$(TZ=Asia/Tokyo date +%Y%m%d)
```

### Step 1: ソースコードをコピー（docs/ 除外）

```bash
rsync -av --exclude='.git' --exclude='node_modules' \
  --exclude='docs' --exclude='public/data/*.json' \
  --exclude='data/' --exclude='README.md' \
  --exclude='walkthrough.md' \
  /Users/igomuni/MyGitHub/marumie-rssystem/ \
  /Users/igomuni/MyGitHub/rs-vis/
```

### Step 2: データファイルをコピー

```bash
cp /Users/igomuni/MyGitHub/marumie-rssystem/public/data/sankey-svg-2024-graph.json.gz \
   /Users/igomuni/MyGitHub/marumie-rssystem/public/data/sankey-svg-2025-graph.json.gz \
   /Users/igomuni/MyGitHub/rs-vis/public/data/
```

### Step 3: 依存関係の更新・ビルド確認

```bash
cd /Users/igomuni/MyGitHub/rs-vis && npm install && npm run build
```

### Step 4: README.md の確認・更新

rs-vis 側の README を確認し、必要な更新があれば手動で反映。

### Step 5: コミット

```bash
cd /Users/igomuni/MyGitHub/rs-vis
git add -A
git commit -m "feat: marumie-rssystem の最新状態を反映（2026-04-29時点）

2026-04-22（#165）以降の開発成果を反映。docs/ は対象外。

- /sankey-svg: 府省庁フィルタ（複数選択）・支出先なしノード・検索連携
- /sankey-svg: Pin機能・関連ノードのみ表示・スクロール操作モード
- /sankey-svg: 予算/支出レンジスライダー・TopNトグル・フィルタUI改善
- /sankey-svg: サイドパネル予算支出併記・ホバーポップアップ改善
- データ: sankey-svg-2024/2025-graph.json.gz を更新"
```

---

## 注意事項

- **必ず rs-vis 側でブランチを作成してから作業する**（main 直コミット禁止）。
- `README.md` は rsync から除外済み。rs-vis 側の公開向け記述を保持するため。
- ビルド後は `/sankey-svg` の府省庁フィルタ・支出先なしノード・検索の動作確認を推奨。

---

## 次回反映時の起点

```bash
git diff --name-only 2b8cf6a HEAD | grep -v '^docs/' | sort
```
