設計書を踏まえて、現状モックが満たしている点／不足している点を整理します:

✅ 設計書と整合している点
トークン化された方針に沿った構造: topToolbarLayout: 'sheet', sidePanelMode: 'bottom-sheet', columnHeaderMode: 'with-amount', controlIconMinHitPx: 44に相当する状態を表現
検索常時表示 + 表示設定をシート集約 (§3 compact-mobile)
ボトムシート3段スナップ (§4 bottom-sheet)
44px最小タップサイズ (§6)
列見出しに金額併記 (with-amountモード、§5)
⚠️ 設計書と乖離している/不足している点
年度ピル型ボタンの扱い — 設計書 §3 では『年度はピル型ボタンで右肩に、フォント/TopN/表示設定はシートに集約』とあります。現モックは年度もシートに入れてしまっており、ここは要修正です。
fullscreen モードへの言及がない — 360px相当（compact-mobileの最小）でのフルスクリーンオーバーレイ案（§4）が提示されていません。
name-only / hidden の列見出しモード — 設計書はモード別の段階的省略を定義しているのに、モックは1パターンのみ。
DisplayMode手動切替の口 — §1で「手動上書き」が基盤要件ですが、UIに現れていません。
修正案
最低限 (1) の「年度ピルを上部右肩に常時表示」は即修正できます。次のような構成に変更します:

[ 検索フィールド (flex-1) ][ 2025 ▾ (年度ピル) ][ ⚙ 設定 ]
年度ピルは44pxで、タップで年度セレクター（小さなポップオーバー or 別シート）を開く想定。これにより「年度は常時見える位置に」という設計書の運用ルールにも合致します。

修正をかけてよいですか? あわせて以下も追加するか確認させてください:

(b)変種: 360px幅でのフルスクリーンノード詳細オーバーレイ案（§4 fullscreen）を4枚目フレームとして追加
列見出し省略モード比較: 同じ初期状態を with-amount / name-only / hidden の3バリアントで並べる小さな比較ストリップ
DisplayMode切替UI: 設定シート末尾に「自動 / コンパクト / 標準 / プレゼン」の手動切替セグメントを追加
どれを採用するか教えてください（全部 or 一部 or (1)のみ）。

はい進めてください、サンキー的なフローが壊れているところも修正してください
You've used 92% of your session limit · It resets at 21:30
Upgrade plan
了解。Sankeyのフロー描画が「stroke-widthでベジエ曲線を太く描く」せいで膨張・突き抜けが起きていました。塗りつぶしの帯状パスで書き直し、設計書との乖離4点も同時修正します。

修正内容:

フロー描画方式刷新 — 上端/下端のペアでベジエ曲線を引いて閉じた帯にする
年度ピルを上部右肩に常時表示（§3）
フレームD追加: 360px幅でのフルスクリーンオーバーレイ案（§4 fullscreen）
列見出しモード比較ストリップ: with-amount / name-only / hidden の3バリアント（§5）
DisplayMode手動切替UIを設定シート末尾に追加（§1）
Now rewriting with proper Sankey path geometry (filled bands, not stroked curves) and all the design-spec fixes. I'll generate flows programmatically with JS so proportions stay correct.

The screenshot shows the old file — looks like cache. Let me force a refresh.

Now I can see the new version rendered properly. Sankey flows are no longer broken (proper filled bands now), all 4 frames present, year pill, DisplayMode segment, column header comparison — all in place.

But I can see one remaining issue: event labels (事業列) and 支出先 labels overlap each other in the middle area because there's not enough horizontal space between columns. Let me fix the column positions and label placement:

You've hit your usage limit — it resets 金 2:30. View your usage details
continue
v2を出しました。主な修正:

Sankeyフロー描画方式刷新: stroke-widthのベジエではなく、上下端ペアの塗りつぶし帯状パスで再描画。突き抜けと膨張を解消。
年度ピル(44h)を常時上部右: 設計書§3「年度はピル型ボタンで右肩に」に整合。
Frame D追加(360px): sidePanelMode: fullscreen のオーバーレイ案。Sankeyを一旦離れて精読する代替フロー。
列見出し3モード比較: with-amount / name-only / hidden を§5通り並列。
DisplayMode手動切替: 設定シート末尾に「自動 / コンパクト / 標準 / プレゼン」セグメント追加。「自動: compact-mobile」チップで現在判定を可視化。
ラベル配置修正: 4カラムをやや左寄せし、支出先ラベルは右ガッターに退避。事業ラベルとの衝突を回避。フォーカス状態では非選択ノードのラベルは非表示にしてノイズを除去。
検証エージェントを別途走らせています(別メッセージで報告)。

検証エージェントから「問題なし」の報告が返ってきました。Mobile Sankey Layout.html は確定版として確認済みです。

次に進めるなら:

タブレット縦 (768px) / 横 (1024px) の compact-tablet モック（§3の「2段ツールバー」案）
アニメーション付きプロトタイプ化 (シート3段スナップの開閉、ノードタップ→詳細遷移)
設計書末尾「採用レイアウト」セクション に組み込めるよう各フレームのスクリーンショット書き出し
どれを進めますか？

> 

設計書 §3〜§6 整合の要点:

2段ツールバー: 1段目に検索+年度ピル、2段目にTopN(事業/支出先 各スライダー)+フォントS/M/L+設定アイコン直接配置
列見出し name-only: ラベルのみ表示、合計は省略（サイドパネルで確認）
サイドパネル2案併記: docked 310px固定（既定）/ bottom-sheet 3段スナップ案
DisplayMode ポップオーバー: 設定アイコン⚙直下に展開、自動/コンパクト/標準/プレゼン + 列見出し + パネル方式を即時切替（モバイルのフルシートと違ってSankeyが見え続ける）
44pxヒットエリア維持 (アイコン/ピル全て)
bottom-sheet 案では半開きを活かして2列リストで事業/支出先を並べて読める
Mobile Sankey Layout.html とデザイン言語（色・タイポ・Sankey帯描画・アイコン・ピル形状）を完全に揃えています。検証エージェントを別途走らせています。