# rs-visとの現在の差分

[rs-vis逆流取り込みの調査](20260912_1158_rs-vis逆流取り込みの調査.md)で洗い出した20コミット分（#331・#332・#333）を反映した後の、両リポジトリの差分状況を記録する。

## 逆流方向（rs-vis→marumie）: 解消済み

rs-vis現HEAD（`3f2be46`）は #331〜#333 で全て反映済み。差分ゼロ。

- #331: フォントサイズの画面幅既定値
- #332: バブルチャートのズームラベル
- #333: 表示範囲UI刷新（TopNSliders→RangeWindowRows、THICKNESS_BOOST等）

対象外とした `infra/terraform/*`（rs-vis専用インフラ）・`playwright.config.ts`/`tests/e2e/*`（rs-vis専用テスト）はそのまま。

## 順方向（marumie→rs-vis）: 大きな未反映分あり

前回の順方向反映（2026-08-11、rs-vis側`610fd99`、marumie側コミット`7180566`相当）以降、**29コミット・206ファイル（docs/.agents/tests/playwright.config.ts除く）・約2.5万行**が未反映。

### 新規ページ（5つ、rs-visにまだ存在しない）

| ページ | 内容 | 主なコミット |
|---|---|---|
| `/mof-jikou` | MOF予算書「事項」一覧、2017〜2026年度・決算対応 | #302, #304, #305 |
| `/mof-kou-moku` | MOF予算書「目」一覧 | #316, #319, #320 |
| `/mof-kou` | MOF予算書「項」一覧 | #316, #319, #320 |
| `/mof-hierarchy` | 事項別内訳の階層サンキー（`/sankey-svg`に操作性を揃えた） | #307, #308, #309, #310, #311, #312, #314 |
| `/mof-sankey` | 予算→項→RS紐づけサンキー（サイドパネルに祖先・RS事業・目タブ、出典リンク） | #322, #325, #326, #327, #328, #330 |

### 主な支援コード

- `app/lib/mof-*` 系: `mof-hierarchy-sankey.ts`（626行）・`mof-kou-sankey.ts`（277行）・`mof-section-rs-sankey.ts`（バイナリ、Sankeyレイアウトのプリコンパイル済みモジュールと思われる）・`mof-sankey-layout.ts`（325行）等、新規サンキー生成ロジック一式
- `app/lib/mof-transfer-sankey-generator.ts`・`app/mof-budget-overview/**`: 既存ページの実データ化・自前SVG移行に伴う大規模書き換え（挿入除去合計で純減、リファクタ）
- `app/lib/mof-rs-kou-moku-linkage-loader.ts`ほか: MOF予算書↔RS事業の紐づけ（事項・目単位）
- テスト: `mof-hierarchy-filter.test.ts`・`mof-hierarchy-focus.test.ts`・`mof-hierarchy-sankey.test.ts`・`mof-sankey-layout.test.ts`（rs-vis側は反映除外対象）

### 新規データファイル（62個）

`public/data/mof-budget-*`・`mof-jikou-*`・`mof-kou-moku-*`・`mof-section-pages-*`（各2017〜2026年度）・`mof-rs-kou-moku-linkage-*`（2023・2024年度）。合計サイズ数MB〜十数MB程度（要実測）。

### 除外・付随変更

- 今回の#331〜#333自体（rs-vis発なのでrs-vis側には元から存在。差分としては相殺される）
- `docs/`配下の調査ドキュメント（一括計上パターン調査・デジタル庁PDF調査等）は反映対象外
- `.gitignore`・`CLAUDE.md`の軽微な変更2件

## 次のアクション（未着手）

`sync-rs-vis`スキルで順方向反映を行う場合、5新規ページを一度に持っていくとビルド確認・動作確認の範囲が大きい。分割するか一括で行うかは未検討。
