# rs-vis の新規変更（スコア再設計・バブルチャート）の逆流取り込み 対応案

作成: 2026-07-28 06:16 JST / 対象: rs-vis `76c6f09`・`1f69d0a` → marumie-rssystem

## 1. 事実確認

### rs-vis 側の未取り込みコミット（2本、著者 ina299・2026-07-28 01:18〜01:34）

| commit | 内容 | 規模 |
|--------|------|------|
| `76c6f09` | 事業バブルチャート `/project-bubble` 追加＋全ページ共通ナビ＋政策評価の統合強化 | 49ファイル・新規コード約 8,000 行＋データ |
| `1f69d0a` | `/model-lab` 廃止、結論を `docs/quality-evaluation-benchmark.md` へ移設 | 3ファイル |

`76c6f09` の内訳は4系統:

- **A: `/project-bubble`** — 事業説明文を `gemini-embedding-001` で埋め込み → UMAP → KMeans。`app/project-bubble/page.tsx`(881)、`client/components/ProjectMap/BubbleCanvas.tsx`(634)、`app/lib/project-map*.ts`、`app/api/project-map`、`scripts/generate-project-map.py`、`public/data/project-map-2025.json.gz`
- **B: 全ページ共通ナビ** — `components/navigation/PageNavMenu.tsx` 新設。sankey-svg 右上を［ツール - 年度 - メニュー］に再構成、表示設定(⋮)を検索ボックス隣へ移動
- **C: sankey 図の政策評価スコア足きり** — 総合点/費用対内容/必要性、URL パラメータ `fso/fsx/fsn`
- **D: AI 政策評価の継続作業** — `app/lib/policy-evaluation.ts`(635)、`/api/policy-summary`・`/api/execution-history`、`app/quality/page.tsx` の大幅改修(+1449)、`scripts/score-project-quality-ai.py` の大改修(+735)、分析スクリプト群、`public/data/project-quality-scores-2025.json.gz` の再生成（995KB→1,307KB）、`rs2025-project-outcomes.json.gz`（8.4MB）新規

### 同期基点は健全

rs-vis `84e6e59`（前回の marumie 反映, #10）のツリーと marumie `7411ff0`（#238）のツリーを比較すると、**コード差分は `scripts/score-project-quality-ai-google.py` の1本のみ**（あとは marumie 側だけにある 254ファイル＝`docs/tasks`・`.agents`・テスト設定等）。つまり rs-vis は marumie の忠実なサブセットで、`84e6e59` を3-way マージのベースに使える。

→ **`git cherry-pick 76c6f09 1f69d0a` が技術的に成立する**（rs-vis をリモート登録済み: `rsvis` = ローカルパス）。

### 衝突の実測（`main` へ試行 cherry-pick、`-X ignore-all-space`）

| ファイル | 衝突hunk | 衝突の中身 |
|---------|---------|-----------|
| `app/sankey-svg/page.tsx` | 8 | 最重量。詳細は下記 |
| `app/quality/page.tsx` | 5 | rs-vis の全面書き換え vs marumie の #242/#245/#248/#262/#266（スコアAPI統合・`ScoreDetailDialog` 分離・SidePanelChrome 共通化・戻りリンク修正） |
| `app/api/quality-scores/route.ts` | 1 | marumie #242 のAPI統合と rs-vis 側の新スキーマ |
| `app/mof-budget-overview/page.tsx` | 1 | 共通ナビ導入 vs marumie #266 の戻りリンク `/sankey-svg` 化 |
| `app/subcontracts/page.tsx` | 1 | 共通ナビ導入 vs marumie #261〜#290 の刷新 |
| `public/data/project-quality-scores-2025.json.gz` | binary | 両側で再生成。**rs-vis 側を採る**（AI再設計後の新世代） |

`app/sankey-svg/page.tsx` の8箇所の性質:

1. L653 / L1126 / L2271 / L2762 — **加算的**。marumie の `filterSubcontract`・`filterRecipientIncludeSub` と rs-vis の `filterScoreO/X/N` を両方残すだけ（依存配列・リセット判定・URL 同期）
2. L1665・L4127 — サイドパネルの事業概要。marumie は共有コンポーネント `ProjectOverviewSection`（#282 で抽出）、rs-vis はページ内直書き＋政策評価サマリ。**共有コンポーネント側に rs-vis の追加表示を寄せる**のが筋
3. L4464・L4905 — **真の設計衝突**。rs-vis の `PageNavMenu` による右上再構成 vs marumie の AI チャットパネル退避オフセット・`isCompactWidth` 分岐・#290 の再委託導線。ここだけは機械マージ不能で設計判断が要る

## 2. 対応案（推奨）

**1本の巨大 PR にせず、衝突リスク順に3本へ分割する。** 全体を1PRにすると sankey-svg の設計衝突がボトルネックになり、独立している `/project-bubble` まで止まる。

### PR-1: 独立追加物（衝突ゼロ）

対象: `app/project-bubble/`、`client/components/ProjectMap/`、`app/lib/project-map*.ts`、`app/api/project-map`・`policy-summary`・`execution-history`、`app/lib/policy-evaluation.ts`、`scripts/*.py`・分析スクリプト群、`docs/*.md`（`policy-decision-scoring.md`・`quality-evaluation-benchmark.md` 等）、`data/exports/*.csv`、新規データ `.gz`、`package.json`・`.gitignore`・`scripts/decompress-data.sh`（自動マージ済み）

手順: cherry-pick 後に衝突6ファイルを `git checkout --ours` で一旦戻し、残りをコミット。`npm run build` と `/project-bubble` の単独動作を確認。

注意点:

- `rs2025-project-outcomes.json.gz` は 8.4MB。**関数バンドル 250MB 問題**（`docs/tasks/20260718_1421_*`）の設計ルールに照らして、`/project-bubble` と `/api/project-map` のトレースに巻き込まれないか `check-traces` で確認する
- `scripts/*.py` は Python 依存（embedding・UMAP・KMeans）。marumie 側に Python パイプラインの運用手順がないため `docs/data-pipeline-guide.md` への追記が要る

### PR-2: 品質スコア再設計の取り込み（UI側）

対象: `app/quality/page.tsx`、`app/api/quality-scores/route.ts`

> **実施時に変更**: `scripts/score-project-quality-ai.py`・`public/data/project-quality-scores-2025.json.gz`・`docs/quality-scoring-guide.md` は PR-1 へ移した（下記「実施記録」参照）。PR-2 は `/quality` の UI 再設計とスコアAPIのみ。

解決方針: **rs-vis 側を基本ベースに採り、marumie 固有の資産（`client/components/quality/*`・`useScoreDetailData`・`SidePanelChrome`・`/sankey-svg` からのダイアログ起動）を上に載せ直す**。rs-vis は marumie の #245〜#262 を知らずに書かれているため、単純な「どちらか採用」では marumie 側の連携が壊れる。

宿題の解消: memory の「2024データは旧世代で再生成が宿題」がここに直結する。`project-quality-scores-2024.json.gz` は rs-vis でも未再生成（626KB）なので、**本 PR では 2025 のみ新世代とし、2024 再生成は別タスク**として明示的に切り出す（年度切替で採点世代が混在する旨を UI かドキュメントに注記）。

### PR-3: 共通ナビ統合＋sankey のスコア足きり

対象: `components/navigation/PageNavMenu.tsx`、`app/sankey-svg/page.tsx`、`app/subcontracts/page.tsx`、`app/mof-budget-overview/page.tsx`

ここで決めること（**要ユーザー判断**）:

- **右上のレイアウト**。rs-vis の［ツール - 年度 - メニュー］案を採るか、marumie の現行（AI チャット退避オフセット・`isCompactWidth`・#290 の再委託導線）を維持して `PageNavMenu` をハンバーガーとしてのみ載せるか。marumie には rs-vis に無い AI チャットパネルがあるため、**rs-vis の配置をそのまま持ってくると退避ロジックと衝突する**
- **表示設定(⋮)の移動**（右上 → 検索ボックス隣）を marumie でも採るか
- スコア足きり `fso/fsx/fsn` は加算的なのでそのまま取り込んでよい

## 3. rs-vis への逆流（取り込み後）

これまで通り「marumie の状態を rs-vis へ反映」する片方向コピー運用を踏襲する。取り込み後は rs-vis 由来の変更が marumie に入っているので、差分は marumie 独自分（#239〜#290：再委託UI刷新、AIチャットBYOK、探索履歴、支出先名寄せ、間接経費ノード等）＋ PR-2/PR-3 のマージ結果になる。

除外パス（現行 rs-vis に存在しないもの＝キュレーション対象）:

```text
app/api/ai/            AIチャットAPI（BYOK・サーバ鍵）
app/api/highlights/
client/components/SankeySvg/AiChatPanel.tsx ほか chat 系
client/lib/ai/
docs/agent-playbook.md, docs/ai-chat-architecture-guide.md, docs/logs/, docs/プロンプトログ*
docs/tasks/            （rs-vis へ展開しない＝コードから参照禁止の理由）
.agents/, .coderabbit.yaml, playwright.config.ts, vitest.config.ts
```

AI チャット（BYOK）を rs-vis にも公開するかは別途判断。従来は非公開扱いなので、**既定は除外を維持**。

反映は `rsvis` リモート経由で marumie 側から push できる（`git push rsvis <branch>`）が、rs-vis 側は `origin` が team-mirai-volunteer のため、実際の PR は rs-vis 作業ツリーから出す。

## 4. 実施順の提案

1. PR-1（独立追加物）→ ビルド確認 → draft PR
2. PR-2（品質スコア）→ `/quality` と `/sankey-svg` のダイアログ連携を実機確認
3. PR-3（ナビ）→ 上記の設計判断を確定してから着手
4. 3本マージ後に rs-vis 逆流の同期コミットを作成

現在のブランチ `feat/subcontract-to-sankey-navigation`（PR #290）は右上導線を触っており PR-3 と直接ぶつかる。**#290 を先にマージしてから PR-3 に着手する**こと。

## 5. 実施記録 — PR-1（2026-07-28）

ブランチ `feat/rsvis-project-bubble-and-policy-eval`。当初案から3点、実装中に境界を動かした。

1. **`public/data/project-quality-scores-2025.json.gz` と `scripts/score-project-quality-ai.py` を PR-1 に含めた**。新API3本と `/project-bubble` は `designClarity`・`evidenceReadiness`・`budgetProportionality`・`necessity`・`policyCategory`・`policyFindings` を読むが、これらは**新世代スコアにしか無い**（旧世代には axis1-5・totalScore まで）。型は全て optional なので `tsc` は通るが、旧データのままだとバブルの推奨判断・大きさが全件 null になり機能しない。生成スクリプトもデータと揃えて入れ、`docs/quality-scoring-guide.md` も同伴させた。旧 `/quality` ページは新データでも動く（読む列は全て残っており、消えた列 `cnVerifiedCount` は非AI版スクリプトとコメントでしか使われていない）ため、UI 再設計を PR-2 に残す境界は保てる。
2. **`components/navigation/PageNavMenu.tsx` を PR-1 に含めた**。`/project-bubble` が import しており、外すと PR-1 単体でビルドが通らない。PR-3 は「既存3ページへの配線」だけになり、設計衝突の範囲がむしろ絞れた。
3. **`components/filters/MinMaxInput.tsx`**: 当初は rs-vis 側の差分（`〜`→`~` の1文字置換）を表記の後退と見て取り込まなかったが、**「rs-vis はそのままの形を維持して取り込む」方針に切り替えたため取り込んだ**（2026-07-28）。

追加で1点修正した。**`rs{YEAR}-project-outcomes.json.gz`（8.4MB）を `data/server` の同期対象から除外**（`scripts/decompress-data.sh`）。AI採点スクリプトの入力専用でサーバ関数からは一切読まないが、`public/data/*.json.gz` を一括コピーする作りのため全30関数のバンドルに乗っていた。除外で関数あたり 45.3MB → 37.2MB。

（PR #291 は当初レビュー対応の修正コミットを含んでいたが、上記の方針転換により `fix/policy-eval-corrections` ブランチへ退避した。既知不具合の一覧は `20260728_0741_rs-vis由来の既知不具合18件の棚卸し.md`。）

検証: `tsc --noEmit` クリーン、`next lint` 新規警告なし（既存警告のみ）、`vitest` 96件パス、`npm run build` 成功、`npm run check-traces` 違反なし。dev サーバで `/api/project-map`・`/api/policy-summary`・`/api/execution-history` の応答と全5ページの 200 を確認。`/api/project-map?year=2025` は 5,794 点すべてに推奨判断が入る（継続2,127 / 要改善1,522 / 条件付き継続1,363 / 縮小444 / 再設計307 / 終了・廃止候補31）。

未着手のまま残したもの: `project-quality-scores-2024.json.gz` は rs-vis でも未再生成のため旧世代のまま。`/project-bubble` は座標ファイルが 2025 のみで 2024 は 404 を返す設計なので実害は出ないが、**年度によって採点世代が混在している**点は PR-2 で `/quality` を再設計するときに扱う。

## 6. 実施記録 — PR-2（2026-07-28）

ブランチ `feat/rsvis-quality-ai-redesign`。**as-is 維持の方針**により、`/quality` は rs-vis 版（1,925行）をそのまま採用した。当初案の「rs-vis をベースに marumie 固有資産を載せ直す」は行っていない。

### スコア詳細ダイアログは2種類が並立する（意図的）

rs-vis 版 `/quality` はダイアログをページ内にインライン定義（644行・政策評価の表示を含む）している。marumie は同じものを `client/components/quality/ScoreDetailDialog.tsx`（515行）へ抽出済みで、`/sankey-svg`・`/subcontracts/[projectId]` からも開ける。

as-is を優先し、**`/quality` はインライン版・他2ページは従来の共有版**という状態で取り込んだ。結果として **サンキー側のダイアログには AI 評価軸が出ない**。共有コンポーネントへの統合は別タスク（`/sankey-svg` と `/subcontracts` の呼び出し側で政策評価データを渡す改修が要るため、PR-3 の領域に踏み込む）。

### `/api/quality-scores` は marumie の層構造に合わせた

rs-vis の route.ts は型定義もローダも1ファイルに同居した旧形。marumie は #242 以降 `app/lib/api/quality-scores-loader.ts` へ分離済みで、`/api/quality-scores/[pid]`・`/api/search/projects` も共用している。rs-vis 版をそのまま採ると marumie の分離が巻き戻り、他2ルートが壊れる。

そこで `76c6f09` が route.ts に加えた**実際の変更だけ**（`startYear`・`endYear`・`noEndDate`・`yearsRunning` の4フィールドと `attachDuration()`）をローダ側へ移植した。ファイル読み込みは marumie の `tryReadDataJson` に置き換えている（`fs`/`zlib` の直叩きをやめる分離の趣旨に合わせるため）。

### rs-vis のコードを marumie の型に合わせた箇所（3件）

いずれも marumie 側が後から厳しくした型・増やした値に追随させたもので、機能の変更ではない。

| 箇所 | 内容 |
|---|---|
| `RecipientRow` の import 元 | `@/app/api/quality-scores/recipients/route` → `@/app/lib/api/quality-recipients-loader`（marumie では型の正典がローダ側） |
| `execAmount` の null | marumie の `QualityScoreItem.execAmount` は欠測に備えて `number \| null`。`formatAmount()` 5箇所と `buildPolicyEvaluations()` の入力で `?? 0` に寄せた。実データは 2024・2025 とも null が0件で、`policy-evaluation` も `execAmount > 0` で判定するため挙動は変わらない |
| `RecipientRow['s']` の `cn` | marumie が #243 で追加した法人番号一致のステータス。rs-vis 版の `STATUS_META` に無く型エラーになるため、marumie 共有ダイアログと同じ扱い（表示上は `valid` と同格の「OK」）で追加 |

検証: `tsc --noEmit` クリーン、`vitest` 96件パス、`npm run build` 成功、`check-traces` 違反なし。dev で全5ページの 200、`/api/quality-scores?year=2025` が 5,794件中 5,682件に `yearsRunning` を返すこと、`/api/quality-scores/recipients?pid=1&year=2025` の応答形が rs-vis 版ページの要求と一致することを確認。

`next lint` に rs-vis 由来の新規警告が2件出る（`react-hooks/exhaustive-deps`・`app/quality/page.tsx` L304/L356）。as-is 維持のため修正していない。

## 7. 実施記録 — PR-3（2026-07-29）

ブランチ `feat/rsvis-nav-and-score-filters`。**rs-vis による右上コントロール群の再構成は取り込まず、機能だけを載せた。**

### 取り込んだもの

- `components/navigation/PageNavMenu`（PR-1 で導入済み）を `/sankey-svg`・`/subcontracts`・`/mof-budget-overview` へ配線
- サンキー図の政策評価スコア足きり（総合点・費用対内容・必要性／URL `fso`・`fsx`・`fsn`）とフィルタパネルの入力UI
- サイドパネルの政策評価ブロック（総合点・費用対内容・必要性、推奨判断／改善アクションのバッジ、`/quality` への導線）
- `/api/policy-summary` の取得と取得失敗時の表示

### 右上の再構成は見送った

`76c6f09` は `app/sankey-svg/page.tsx` の右上を［ツール - 年度 - メニュー］へ組み替え、表示設定(⋮)を検索ボックス隣へ移す。marumie の右上は **AIチャットパネル展開時に `rightControlsOffset` ぶん左へ退避する制御**と `isCompactWidth` 分岐、探索履歴の入口を抱えており、rs-vis にはこれらが無い。そのまま持ち込むと退避ロジックと衝突し、年度セレクタが二重になる。

そこで、差分から次の3ハンクを除外して適用した（`git diff 84e6e59 76c6f09 -- app/sankey-svg/page.tsx` の該当ハンク）:

```text
@@ -3991,24 +4254,6   中央クラスタ（年度セレクタ＋探索履歴）の削除
@@ -4326,6 +4605,91   表示設定(⋮) を検索ボックス隣へ移動
@@ -4381,207 +4745,30  右上クラスタの全面置換
```

`PageNavMenu` は既存の表示設定ボタンの左隣（`right: 52 + rightControlsOffset`）に置き、退避のトランジションを合わせた。**配置の最終形は現物を見てから調整する**前提。

### スコア足きりは `buildFilterExcludedIds` のフックとして実装した

rs-vis はフィルタ除外ロジックをページ内にインラインで持ち、そこに `failScore` を足している。marumie は #242 以降このロジックを `app/lib/sankey-query.ts` の `buildFilterExcludedIds` へ抽出済みで、`/api/sankey/query` と共用しユニットテストも持つ。rs-vis 版をそのまま採ると抽出が巻き戻り、再委託フィルタ（`subcontract.minDepth`・`recipientName.includeSubcontract`）も失われる。

そこで `buildFilterExcludedIds` に **事業単位の追加除外フック** `excludeProject?: (projectId: number) => boolean` を足し、画面側で `policySummary` から作った述語を渡す形にした。API のクエリスキーマ（`ResolvedSankeyQuery['filter']`）は変えていない。スコアは画面側にしか無いデータなので、スキーマに載せるのは筋が悪いという判断。フックの動作（対の除外・省庁へのカスケード・空なら null）はユニットテスト4件を追加した。

### 細かい追随

- `app/subcontracts/page.tsx`: rs-vis は年度変更時に `router.replace` を呼ぶが、marumie は `history.replaceState` で年度を含む全状態をURL同期済み（#263）。重複するため `router.replace` は入れていない
- `app/sankey-svg/page.tsx`: rs-vis が追加する `projectDetailCache` の `useState` は、marumie の `useProjectPidCache` と重複するため取り込んでいない

検証: `tsc --noEmit` クリーン、`next lint` エラーなし、`vitest` 100件パス（+4）、`npm run build` 成功、`check-traces` 違反なし。dev で全5ページ 200、`PageNavMenu` が `/sankey-svg`・`/subcontracts`・`/project-bubble` の初期HTMLに出ること（`/quality`・`/mof-budget-overview` は初期ローディング画面を返す構造のため描画後に出る）を確認。

### PR-3 のレビュー指摘（2026-07-29・#294）

**これまでの18件と性質が違う。** rs-vis 由来のバグではなく、**右上の再構成3ハンクを除外したことで生じた取り込み側の後始末漏れ**であり、どちらも `next lint` に新規警告として出ている（このリポジトリは「新規警告を増やさない」運用）。

| 指摘 | 内容 |
|---|---|
| `offsetControlsBlock` が未使用（Major） | `app/sankey-svg/page.tsx` L3071-3187 の117行。rs-vis がオフセットスライダーを変数へ抽出したもので、それを描画するハンク（`@@ -4381,207`）を除外したため宣言だけが残った。実際に描画されているのは L4881 以降の既存JSX。lint 警告 `'offsetControlsBlock' is assigned a value but never used` |
| スコア足きり述語の置き場所（Trivial） | `scoreExcludeProject` の閾値パース・比較は純ロジックなので `app/lib` 側へ、という指摘。Layer Design Rules（ページは状態管理・API呼び出し・レイアウトのみ）に照らすと妥当 |

あわせて、`filterExcludedIds` の依存配列に足した `filterScoreO`/`filterScoreX`/`filterScoreN` が冗長になっている（スコアは `scoreExcludeProject` 経由でしか参照しないため）。lint 警告 `useMemo has unnecessary dependencies`。

**対応方針**: 2026-07-29 時点でユーザー判断により見送り。ただし1件目は117行の純粋な削除で挙動に影響がなく、2件目・3件目も小さい。**次に `app/sankey-svg/page.tsx` を触るとき（品質スコアデザインの展開タスク）に片付けるのが自然。**
