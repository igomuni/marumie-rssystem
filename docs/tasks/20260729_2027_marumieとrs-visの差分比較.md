# marumie ↔ rs-vis の差分比較（2026-07-29 時点）

比較対象: marumie `3c0224f`（#294 マージ後） ↔ rs-vis `1f69d0a`
前回同期: rs-vis `84e6e59`（#10・2026-06-23・marumie `#231` 相当）

## 結論

**逆流（rs-vis → marumie）は完了している。rs-vis にしか無いファイルは0件。** 残るのは片方向（marumie → rs-vis）の未反映分で、前回同期以降 **marumie に47コミット**が積まれている。

| 区分 | 件数 |
|---|---|
| rs-vis のみに存在 | **0** |
| marumie のみに存在 | 384（うち `docs/tasks` 297・コード/設定 **87**） |
| 両方にあり内容が異なる | 49（うち `docs/` 除く **41**） |

## 1. marumie のみに存在するコード・設定（87件）

系統別に整理する。

### AIチャット（BYOK）— 一式まるごと未反映

`app/api/ai/sankey-chat/`、`app/lib/ai/*`（chat-core・sankey-chat-agent・tool-executor-server・tool-shaping）、`client/lib/ai/*`（byok-chat・openrouter-caller・api-key-store・client-tool-executor・chat-history-store）、`client/components/SankeySvg/AiChatPanel.tsx`・`ChatMarkdown.tsx`、`types/sankey-ai-chat.ts`、`scripts/dev/ai-chat-eval*`、`docs/ai-chat-architecture-guide.md`

**「意図的に除外している」わけではない。** `.agents/skills/sync-rs-vis/SKILL.md` の rsync 除外リストに AI 系の指定は無く、BYOK 対応（#268）が前回同期より後に入ったため単に未反映なだけ。以前 `20260728_0616_rs-vis逆流取り込みの対応案.md` に「既定は除外を維持」と書いたが、**根拠は無かったので撤回する**。公開するかは判断が要る（鍵の扱い・CSP を含むため）。

### 探索履歴・発見メモ

`client/components/SankeySvg/ExplorationHistory.tsx`、`client/lib/exploration-store.ts`、`app/lib/exploration-label.ts`、`client/lib/relative-time.ts`

### 再委託UI刷新（#261〜#290）の共有部品

`client/components/subcontract/*`（BlockInspector・ProjectOverviewSection・origin-kind）、`client/components/BudgetExecutionSection.tsx`、`client/components/SidePanelChrome.tsx`、`client/components/TagChip.tsx`・`tag-chip-colors.ts`、`app/lib/subcontract-ribbon-layout.ts`、`app/lib/subcontracts/indirect-costs.ts`、`client/hooks/useSidePanel.ts`・`useBaseFontPx.ts`

### 品質スコアの共有コンポーネント

`client/components/quality/ScoreDetailDialog.tsx`・`QualityScoreBlock.tsx`・`score-format.ts`、`client/hooks/useScoreDetailData.ts`

rs-vis 側は `/quality` にダイアログをインライン定義しており、共有版を持たない。`20260729_1827_品質スコアデザインをサンキー図と再委託ビューへ展開.md` の対象そのもの。

### API・ローダの層分離（#231〜#242 以降）

`app/lib/api/*`（data-file・highlights-loader・project-details-loader・quality-recipients-loader・sankey-graph-loader・subcontracts-loader・usage-log）、`app/api/quality-scores/[pid]/`、`app/api/sankey/query/`、`app/api/search/spending/`、`app/api/highlights/`、`app/lib/sankey-query.ts`、`app/lib/search/spending-search.ts`、`types/sankey-query.ts`

### テスト・開発基盤

`vitest.config.ts`、`playwright.config.ts`、`tests/e2e/*`、`app/lib/**/*.test.ts`（10ファイル・100ケース）、`scripts/check-function-traces.mjs`、`.coderabbit.yaml`、`.agents/skills/*`

**`.agents/` と `tests/` は sync skill が明示的に除外している**ので、これは意図どおり。

### 支出先の法人番号解決（#243）

`scripts/generate-recipient-resolution.py`、`scripts/analyze-houjin-quality.py`、`public/data/recipient-resolution-{2024,2025}.json.gz`

## 2. 両方にあって内容が異なる主なファイル（41件）

| ファイル | 差分行 | 何が違うか |
|---|---|---|
| `app/subcontracts/[projectId]/page.tsx` | 2,307 | 再委託UI刷新 Phase1〜4（#261〜#290）が丸ごと未反映 |
| `app/sankey-svg/page.tsx` | 1,779 | marumie の AIチャット・探索履歴・再委託フィルタ・共有パネル ＋ **PR-3 で見送った rs-vis の右上再構成** |
| `app/lib/sankey-query.ts` | 839 | marumie のみに存在（クエリAPIの抽出） |
| `app/api/quality-scores/route.ts` | 210 | marumie はローダへ分離済み、rs-vis は1ファイル同居の旧形 |
| `app/lib/api/quality-scores-loader.ts` | 125 | marumie のみ |
| `app/subcontracts/page.tsx` | 137 | URL=状態化（#263）ほか |
| `next.config.ts` | 56 | 関数バンドル制御（`outputFileTracingExcludes`/`Includes`）・CSP |
| `app/quality/page.tsx` | **26** | PR-2 の型追随3件のみ（下記） |
| `package-lock.json` | 2,735 | 依存差 |

### `app/quality/page.tsx` の残差は PR-2 の型追随3件だけ

PR-2 で as-is 取り込みしたため、差分は marumie 側で入れた次の3点に収束している。

- `RecipientRow` の import 元（marumie は `app/lib/api/quality-recipients-loader`）
- `execAmount` の `?? 0`（marumie の型が `number | null`）
- `STATUS_META` の `cn`（#243 の法人番号一致）

**いずれも marumie 側のローダ・型が rs-vis に無いことに起因する。** 87件を一緒に反映すれば自然に解消し、rs-vis 側で個別対応は要らない。

## 3. データファイル（`public/data/*.gz`）

marumie が全て新しい。

```text
sankey-svg-2024-graph.json.gz          1,340,460 → 1,756,303  (+31%)
sankey-svg-2025-graph.json.gz          1,425,942 → 1,850,915  (+30%)
project-quality-recipients-2024.gz     3,093,597 → 3,483,187  (+13%)
project-quality-recipients-2025.gz     3,196,489 → 3,602,876  (+13%)
recipient-index-{2024,2025}.gz         微増
project-quality-scores-2024.json.gz      626,970 →   629,602
recipient-resolution-{2024,2025}.gz          なし → 新規
```

sankey graph の +30% は間接経費の終端ノード（#288）・再委託階層（#275）など、グラフに載る情報が増えたため。

## 4. 反映の手順は既にある

`.agents/skills/sync-rs-vis/SKILL.md` が正典。rsync ベースで `docs/`・`.agents/`・`tests/`・`playwright.config.ts`・`README.md`・`data/`・`public/data/*.json` を除外し、`.gz` は個別コピー、rs-vis 側で `npm run build` を通してから PR を出す流れ。

ただし**手順1（前回反映コミットの特定）が機能しない**。`docs/tasks/` 直下に `*rs-vis反映対応案.md` が無く、`archive/` に3本（最新 `20260528_1853`）あるだけで、その後の #10 反映（2026-06-23）に対応する記録が残っていない。次に反映するときは、この文書を起点記録として使えばよい。

**起点は marumie `7411ff0`（#238）** — rs-vis `84e6e59` のツリーとコード差分1ファイル（`score-project-quality-ai-google.py`）まで一致することを確認済み。

## 5. 反映前に決めること

1. **AIチャット（BYOK）を公開するか。** 一式で25ファイル規模。CSP（#269）と鍵の扱いが絡むため、コード反映だけでなく公開ポリシーの判断が要る
2. **既知不具合18件を先に当てるか。** `fix/policy-eval-corrections`。反映後に rs-vis へ別途当てるより、marumie で先に潰してから反映するほうが手数が少ない（`20260728_0741_rs-vis由来の既知不具合18件の棚卸し.md`）
3. **`docs/` の扱い。** sync skill は除外しているが、rs-vis には古い `docs/tasks/archive` が残っている（前回除外を入れる前の残骸）。整理するかは別途
